# Time & Pay Tracker

Fresh Expo SDK 57 build of the Time & Pay Tracker mobile app.

See `SETUP-SDK57.md` before installing.

This package intentionally contains no `node_modules` directory and no
package lockfile so it cannot inherit the previous SDK 54/57 mixed install.
