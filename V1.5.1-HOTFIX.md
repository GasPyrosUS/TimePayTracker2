# V1.5.1 Hotfix

Fixed:
- Dashboard render error: `Property 'localDateString' doesn't exist`.
- V1.5 no longer needs `localDateString()` on the Dashboard because the
  CLOCK IN button now reads the persisted active clock session directly.

Preserved:
- Persistent clock-in across navigation and app restarts.
- Editable/deletable saved time entries.
- Dashboard/pay synchronization.
- Adjustable pay periods.
- Light/Dark/System appearance.
- Existing time entries, settings, and appearance data.
