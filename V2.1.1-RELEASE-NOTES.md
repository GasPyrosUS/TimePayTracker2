# 2.1.1 — Web entry deletion

- Fixes the web DELETE ENTRY button: React Native Web's Alert.alert has no implementation.
- Uses an explicit browser confirmation for web; Android/iOS keep the native dialog.
- Cancel/dismiss never deletes. Repeated clicks are blocked while deletion is pending.
- Shows an inline error if storage fails; keeps account-change guards and normal sync notifications.
- Saved time-card snapshots are unchanged, as before. Team deletions sync when Firebase is connected.
- No Firestore rule or dependency changes are needed for this fix.
